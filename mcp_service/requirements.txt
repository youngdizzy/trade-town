# TradeTown Read-Only MCP Boundary 1.0 — ISOLATED service dependencies.
#
# THIS FILE MUST NEVER BE MERGED INTO backend/requirements.txt.
#
# mcp 2.2.0 requires pydantic>=2.12.0 and resolves to pydantic 2.13.5.
# backend/requirements.txt pins pydantic==2.10.2, and backend/app/schemas.py
# is ~15,700 lines of Pydantic models gated by mypy, ruff and 194 test files.
# Installing the MCP SDK into the backend would force that upgrade. Running
# the MCP server as a separate process with its own dependency set is
# therefore not only the chosen architecture — it is the only mechanically
# possible one. Verified by resolution in a clean python:3.12-slim container.
#
# Fully pinned, matching this repo's existing convention of pinning every
# runtime dependency exactly.
mcp==2.2.0
mcp-types==2.2.0

# Resolved transitive set, pinned for reproducibility.
PyJWT==2.13.0
annotated-types==0.8.0
anyio==4.15.1
attrs==26.1.0
cffi==2.1.1
click==8.5.0
cryptography==50.0.1
h11==0.16.0
httpcore2==2.12.0
httpx2==2.12.0
idna==3.19
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
opentelemetry-api==1.44.0
pycparser==3.0
pydantic==2.13.5
pydantic_core==2.46.5
python-multipart==0.0.32
referencing==0.37.0
rpds-py==2026.6.3
sse-starlette==3.4.11
starlette==1.6.0
truststore==0.10.4
typing-inspection==0.4.4
typing_extensions==4.16.0
uvicorn==0.52.4
